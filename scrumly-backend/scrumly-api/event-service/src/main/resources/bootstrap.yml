spring:
  application:
    name: event-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888
      username: DEV_CONFIG_USERNAME
      password: DEV_CONFIG_PASSWORD
      name: event-service, common


default-path:
  activity-types: default/default-activity-type.json
  activity-templates: default/default-templates.json

