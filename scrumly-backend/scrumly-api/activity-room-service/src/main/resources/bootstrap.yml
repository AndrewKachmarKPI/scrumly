spring:
  application:
    name: activity-room-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888
      username: DEV_CONFIG_USERNAME
      password: DEV_CONFIG_PASSWORD
      name: activity-room-service, common

