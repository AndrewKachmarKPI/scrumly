spring:
  application:
    name: integration-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888
      username: DEV_CONFIG_USERNAME
      password: DEV_CONFIG_PASSWORD
      name: integration-service, common
